import React from 'react';
import styled, {css} from 'styled-components';

const PricingLine = styled.div`
  display: flex;
  flex-wrap: nowrap;
  justify-content: space-between;
`

export default PricingLine;